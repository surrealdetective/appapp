<p id="notice"><%= notice %></p>

<h2>User Info</h2>

<p>
  <b>Name:</b>
  <em><%= @user.name %></em>
</p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>


<h2>Dossier Info</h2>

<p>
  <b>Tagline:</b>
  <%= @user.last_dossier.tagline %>
</p>


<h2>Dossier Timeline</h2>
<ul>
  <% @user.last_dossier.dossier_statuses.each do |status| %>
  <li><%= status.state %> on <%= status.updated_at %> 
  </li>    
  <% end %>
  
</ul>


<%= link_to 'Edit User Info', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
