<!-- NAVBAR -->
<div class="navbar navbar-inverse">
  <div class="navbar-inner">
    <button type="button" class="btn btn-navbar visible-phone" id="menu-toggler">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to (image_tag("flatiron_school_logo.png")), :root, {:class => "brand"} %>
    <ul class="nav pull-right">

      <%# appropriate way to check if someone is logged in?  %>
      <% if current_user %>

        <%# feels appropriate to use this can? because searching leads to reading %>
        <% if can? :manage, Dossier %>
          <%= render "layouts/search" %>
        <% end %>

        <%= render "layouts/dropdown" %>

      <% end %>

    </ul>
  </div>
</div> <!-- end navbar>